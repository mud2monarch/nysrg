# World models

https://dl.acm.org/doi/epdf/10.1145/122344.122377

- The action model, which is apparently not part of this paper, seems like a critical piece of the puzzle. It's the 'eval' of modern RL
- What are the limits to a world model? what distinguishes this from a vanilla ML setup?
- If you had a comprehensive IDP experiemnt could you just statically analyze then code the optimal behaviors?
- Waht is the scale of systems being trained here?
- In terms of perceiving and storing state, this does seem like a hard problem requiring a lot of storage. I wonder how big the state gets at times.
- I guess the hard part of implementation is:
  - Collecting and sharing state
  - Designing an appropriate reward maximization function, e.g., a robot that picks up trash but needs to recharge itself when low battery
  - Designing an appropriate feedback loop and reward system
  
- The discussion points out that the table/DP model breaks down the second you have probabilistic state improvement or non-determinism. And I supposed the implication is that most meaningful problems have that. For example, in chess, it's hard to give a strict scoring to a move because we don't have th efull set of scores for every arrangement of chess, and because a given state can change in so many ways.

https://worldmodels.github.io/

"A small controller lets the training algorithm focus on the credit assignment problem on a small search space, while not sacrificing capacity and expressiveness via the larger world model. By training the agent through the lens of its world model, we show that it can learn a highly compact policy to perform its task."

this reminds me of MoE! You have a big space and then use something smaller but more precise to manipulate the big things. or fusion routing, etc.

- Paper distinguishes this from non-model rl techniques. i don't know what those are, let me ask.
  - oh interesting, so non-model rl doesn't have 'planning' necessarily. so a robot is presented with a state and doesn't think 'okay if i go to this state then this state then this state then this state ill get this reward so that's good'. that requires a model of the world. instead, it just learns, over time, "given this state, it's better to do Y"
  - The llm says that this 'amortizes' (really, 'shifts'?) the cost of planning from runtime to training time, and number of iterations (you need to get sufficiently large n for many many situations)
  - There's a nice re-use of the vision encoding to add time dimensionality to this.
    - You need to encode an image into a tensor, so you need the VAE. This gives you the vision state
    - But you also want to give some change-over-time information to the 
  - VAE = VARIATIONAL autoencoder. Variational = the output is a probability density function rather than a single (deterministic) output. the PDF output is better for tasks that are inherently stochastic/probabilistic. e.g., if you look at a scene from coordinate (x, y) that's probably very similar to (x+0.00001,y), so we really want to take the PDF over these instead of the specific tensor.
  - Greg is interested in whether you can 'random index' into the output of a VAE