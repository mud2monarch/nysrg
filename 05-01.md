pub = a tool made for Dart
grub = a colloquialism for grabbing that rhymes with grub?

- What's the difference between pubgrub and a SAT Solver?
- Does UV Lock semantics match pubgrub semantics?

Okay, so I'm learning that a SAT solver is a general category of tools that can resolve conflicts between boolean statements.

PubGrub is distinct in a few ways:
- Good logging and error messages specific to dependency resolution
- Faster because you can rely on ranges

PubGrub rust crate, by default, only resolves to a single version per dependency.

